<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="ZoDream.Authenticator.Pages.StartupPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:ZoDream.Authenticator.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:ZoDream.Authenticator.ViewModels"
    mc:Ignorable="d">

    <Page.DataContext>
        <vm:StartupViewModel />
    </Page.DataContext>

    <Grid>
        <StackPanel
            Width="250"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
            <TextBox PlaceholderText="Master Password" Text="{Binding Account, Mode=TwoWay}" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="auto" />
                </Grid.ColumnDefinitions>
                <TextBox PlaceholderText="Key File" Text="{Binding Password, Mode=TwoWay}" />
                <Button Grid.Column="1">
                    <FontIcon Glyph="&#xE8E5;" />
                </Button>
            </Grid>
            <Button
                HorizontalAlignment="Stretch"
                Command="{Binding EnterCommand}"
                Content="Enter" />
        </StackPanel>
    </Grid>
</Page>
